# inksplat's environment — copy to .env (gitignored) for local prod-mode runs.
# A stateless app (ADR 0008) needs no DATABASE_URL — only the port is optional.
# In production these come from Fly, never from a committed file.

# Port the prod server listens on (default 8080).
# PORT=8080
