# Database environment — validated by `loadDbEnv()` from @hoe/db/env.
# Copy to .env (gitignored) and fill in real values. In production these come
# from Fly secrets, never from a committed file.

# Postgres connection string for the app's own database (Fly MPG in prod).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
